{
  "Logging": {
    "LogLevel": {
      "Default": "Warning", // General level for the whole application to reduce verbosity
      "Microsoft": "Warning", // Logs only warnings and errors from Microsoft libraries
      "Microsoft.Hosting.Lifetime": "Information", // Keeps app lifecycle events visible
      "SecurityMaster_API.Controllers.EquityController": "Information", // Controller-specific logs
      "SecurityMaster_API.Controllers.BondController": "Information", // Include logs for BondController
      "SecurityMaster_API": "Information", // Logs events specific to your API
      "System": "Error" // Restrict System logs to errors for reducing noise
    }
  }
}